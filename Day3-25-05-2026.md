# Today's GitHub Log - 25/05/2026 - Day 3

## AWS

### Create Subnet in Default VPC

#### Get AWS Credentials

```bash id="w7x2lb"
showcreds
```

#### Export Credentials

```bash id="y2v5nr"
export AWS_ACCESS_KEY_ID=XXXXXXXX
export AWS_SECRET_ACCESS_KEY=XXXXXXXX
export AWS_DEFAULT_REGION=us-east-1
```

#### Get Default VPC

```bash id="e4m8qt"
aws ec2 describe-vpcs \
--filters "Name=isDefault,Values=true" \
--query "Vpcs[0].VpcId" \
--output text
```

#### Create Subnet

```bash id="f1k3zu"
aws ec2 create-subnet \
--vpc-id <VPC-ID> \
--cidr-block 10.0.1.0/24 \
--availability-zone us-east-1a \
--tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=devops-subnet}]'
```

#### Verify Subnet

```bash id="z8c9mn"
aws ec2 describe-subnets \
--filters "Name=tag:Name,Values=devops-subnet" \
--output table
```

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

# Azure

## Create Azure VM using Azure CLI

### Check Login

```bash
az account show
```

### List Resource Groups

```bash
az group list --output table
```

### Create VM

```bash
az vm create \
  --resource-group <RESOURCE-GROUP> \
  --name devops-vm \
  --image Ubuntu2204 \
  --size Standard_B2s \
  --admin-username azureuser \
  --generate-ssh-keys \
  --storage-sku Standard_LRS \
  --os-disk-size-gb 30
```

### Verify VM Running State

```bash
az vm get-instance-view \
  --resource-group <RESOURCE-GROUP> \
  --name devops-vm \
  --query "instanceView.statuses[?starts_with(code, 'PowerState/')].displayStatus" \
  --output tsv
```

### Get Public IP

```bash
az vm list-ip-addresses \
  --resource-group <RESOURCE-GROUP> \
  --name devops-vm \
  --output table
```

### SSH into VM

```bash
ssh azureuser@<PUBLIC-IP>
```

### Fix PEM Permissions

```bash
chmod 400 mykey.pem
```

### SSH using PEM Key

```bash
ssh -i mykey.pem azureuser@<PUBLIC-IP>
```

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

# Terraform

## Create AWS VPC

### Navigate to Terraform Directory

```bash
cd /home/bob/terraform
```

### Create/Edit main.tf

```bash
vi main.tf
```

### Terraform VPC Configuration

```hcl
resource "aws_vpc" "datacenter_vpc" {
  cidr_block = "10.0.0.0/16"

  tags = {
    Name = "datacenter-vpc"
  }
}
```

### Initialize Terraform

```bash
terraform init
```

### Apply Terraform

```bash
terraform apply -auto-approve
```

### Verify Resources

```bash
terraform state list
```

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

# DevOps / Linux

## Disable Root SSH Login

### SSH into App Server

```bash 
ssh tony@stapp01
```

### Become Root

```bash 
sudo su -
```

### Edit SSH Config

```bash 
vi /etc/ssh/sshd_config
```

### Change Setting

```bash
PermitRootLogin no
```

### Restart SSH Service

```bash 
systemctl restart sshd
```

### Verify Setting

```bash
grep PermitRootLogin /etc/ssh/sshd_config
```

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

# MLOps / Python Ecosystem

## UV Dependency Compilation

### Go to Project Directory

```bash
cd /root/code/fraud-detection
```

### Check Existing File

```bash
cat requirements.in
```

### Edit requirements.in

```bash
vi requirements.in
```

### Corrected requirements.in

```txt
scikit-learn>=1.5.0
mlflow>=2.0.0
pandas>=2.0.0
numpy>=1.26.0
```

### Compile Lockfile

```bash
uv pip compile requirements.in -o requirements.txt
```

### Verify Output

```bash
cat requirements.txt
```

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

# Summary

| Area      | Topics Practiced                                         |

| AWS       | VPC, Subnet, Security Group, AWS CLI                     |
| Azure     | VM provisioning, SSH access                              |
| Terraform | AWS VPC provisioning                     |
| Linux     | SSH hardening, user/security management                  |
| MLOps     | Python dependency locking, uv, reproducible environments |
| DevOps    | Automation, troubleshooting, infrastructure setup        |
