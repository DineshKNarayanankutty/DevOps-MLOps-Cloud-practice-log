# Today's Log — 17/07/2026 - Day47

Today's work focused on **Amazon SNS/SQS priority queuing with CloudFormation, Azure SQL Database backup and export, Dockerizing a Python application, Great Expectations data drift validation, Kubernetes Job creation, and Linux forensic file collection**.

---

# AWS

# Amazon SNS + SQS Priority Queue using CloudFormation

## Objective

Deploy a priority-based messaging system using AWS CloudFormation consisting of Amazon SNS, Amazon SQS, AWS Lambda, and IAM. The solution routes messages into separate high- and low-priority queues and ensures the Lambda function processes high-priority messages before low-priority ones.

---

## Resources Created

```text
CloudFormation Stack:
devops-priority-stack

SNS Topic:
devops-Priority-Queues-Topic

SQS Queues:
devops-High-Priority-Queue
devops-Low-Priority-Queue

Lambda Function:
devops-priorities-queue-function

IAM Role:
lambda_execution_role
```

---

## Architecture

```text
                SNS Topic
      devops-Priority-Queues-Topic
                    │
        ┌───────────┴───────────┐
        │                       │
 priority = high          priority = low
        │                       │
        ▼                       ▼
High Priority Queue      Low Priority Queue
        │                       │
        └───────────┬───────────┘
                    ▼
          Lambda Function
devops-priorities-queue-function
                    │
       Poll High Queue First
                    │
      If Empty → Poll Low Queue
```

---

## CloudFormation Components

Created:

```text
Amazon SNS Topic

Amazon SQS Queues

SNS Queue Subscriptions

Queue Policies

Lambda Function

Lambda Execution Role

Event Source Mapping
```
## CloudFormation Template

```
AWSTemplateFormatVersion: '2010-09-09'

Description: Priority Queue using SNS SQS Lambda

Resources:

  HighPriorityQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: devops-High-Priority-Queue

  LowPriorityQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: devops-Low-Priority-Queue

  PriorityTopic:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: devops-Priority-Queues-Topic

  HighSubscription:
    Type: AWS::SNS::Subscription
    Properties:
      TopicArn: !Ref PriorityTopic
      Protocol: sqs
      Endpoint: !GetAtt HighPriorityQueue.Arn
      FilterPolicy:
        priority:
          - high

  LowSubscription:
    Type: AWS::SNS::Subscription
    Properties:
      TopicArn: !Ref PriorityTopic
      Protocol: sqs
      Endpoint: !GetAtt LowPriorityQueue.Arn
      FilterPolicy:
        priority:
          - low

  HighQueuePolicy:
    Type: AWS::SQS::QueuePolicy
    Properties:
      Queues:
        - !Ref HighPriorityQueue
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: "*"
            Action: sqs:SendMessage
            Resource: !GetAtt HighPriorityQueue.Arn
            Condition:
              ArnEquals:
                aws:SourceArn: !Ref PriorityTopic

  LowQueuePolicy:
    Type: AWS::SQS::QueuePolicy
    Properties:
      Queues:
        - !Ref LowPriorityQueue
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: "*"
            Action: sqs:SendMessage
            Resource: !GetAtt LowPriorityQueue.Arn
            Condition:
              ArnEquals:
                aws:SourceArn: !Ref PriorityTopic

  LambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: lambda_execution_role
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
            Action:
              - sts:AssumeRole

      ManagedPolicyArns:
	  - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole	
	  - arn:aws:iam::aws:policy/AmazonSQSFullAccess
	  - arn:aws:iam::aws:policy/AmazonSNSFullAccess

  PriorityLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: devops-priorities-queue-function
      Runtime: python3.12
      Handler: index.lambda_handler
      Role: !GetAtt LambdaExecutionRole.Arn
      Timeout: 10

      Code:
        S3Bucket: devops-s3bucket-121121
        S3Key: function.zip
```

---

## Lambda Logic

The Lambda function was configured to:

1. Poll the high-priority queue.
2. Delete the processed message.
3. Poll the low-priority queue only when the high-priority queue is empty.

Core logic:

```python
response = poll_messages(high_priority_queue)

if response == "No more messages to poll":
    response = poll_messages(low_priority_queue)
```

---

## Testing

Published messages using SNS with message attributes:

```bash
aws sns publish \
--topic-arn $topicarn \
--message "High Priority message 1" \
--message-attributes '{"priority":{"DataType":"String","StringValue":"high"}}'
```

Repeated for:

```text
High Priority message 2

Low Priority message 1

Low Priority message 2
```

---

## Verification

Verified:

```text
CloudFormation Stack:
CREATE_COMPLETE

SNS Topic:
Created

SQS Queues:
Created

Lambda Function:
Successfully Invoked

High Priority Messages:
Processed First

Low Priority Messages:
Processed After High Queue Became Empty
```

---

## Key Learning

SNS message filtering combined with separate SQS queues provides an effective priority-based messaging pattern. By polling the high-priority queue before the low-priority queue, critical messages are always processed first without modifying the producers.

---

# Azure

# Azure SQL Database Backup using BACPAC Export

## Objective

Provision an Azure SQL Database, export it to Azure Blob Storage as a BACPAC file, and download the backup to the client machine.

---

## Resources Created

```text
SQL Server:
nautilus-server-24583

SQL Database:
nautilus-sqldb

Storage Account:
nautilusst23876

Blob Container:
nautilus-container-411
```

---

## SQL Database Configuration

```text
Database Name:
nautilus-sqldb

Server:
nautilus-server-24583

Location:
West US

Compute Tier:
Basic

Maximum Size:
2 GiB

Backup Storage:
Locally Redundant (LRS)

Authentication:
SQL Authentication
```

---

## Backup Process

Exported the database using:

```text
Azure SQL Database

↓

Export

↓

Storage Account

↓

Blob Container
```

Generated backup:

```text
nautilus-db-backup.bacpac
```

---

## Download

Downloaded using Azure CLI:

```bash
az storage blob download \
--account-name nautilusst23876 \
--container-name nautilus-container-411 \
--name nautilus-db-backup.bacpac \
--file /opt/nautilus-db-backup.bacpac
```

---

## Verification

Verified:

```text
Database Status:
Online

Blob Container:
Backup Present

Downloaded File:
/opt/nautilus-db-backup.bacpac
```

---

## Architecture

```text
Azure SQL Database
        │
        ▼
Export (BACPAC)
        │
        ▼
Blob Container
        │
        ▼
Azure Client Host
        │
        ▼
/opt/nautilus-db-backup.bacpac
```

---

## Key Learning

Azure SQL Database exports are stored as **BACPAC** files, enabling schema and data portability for backup, migration, and restoration scenarios.

---

# DevOps

# Dockerizing a Python Application

## Objective

Containerize a Python application using Docker and expose it through host port **8097**.

---

## Dockerfile

Created:

```dockerfile
FROM python:3.9-slim

WORKDIR /app

COPY src/requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY src/ .

EXPOSE 5000

CMD ["python", "server.py"]
```

---

## Image Build

Executed:

```bash
docker build -t nautilus/python-app .
```

---

## Container Deployment

Created container:

```bash
docker run -d \
--name pythonapp_nautilus \
-p 8097:5000 \
nautilus/python-app
```

---

## Verification

Checked:

```bash
docker ps
```

Tested:

```bash
curl http://localhost:8097/
```

Received the application response successfully.

---

## Architecture

```text
Docker Host
      │
      ▼
Python Container
(pythonapp_nautilus)
      │
Host Port 8097
      │
      ▼
Container Port 5000
      │
      ▼
Python Application
```

---

## Key Learning

Separating dependency installation from application source copying improves Docker layer caching, resulting in faster rebuilds when only application code changes.

---

# MLOps

# Great Expectations Data Drift Investigation

## Objective

Investigate a failed **drift_check** validation using Great Expectations Data Docs, widen the failing expectation, and restore successful validation without removing any expectations.

---

## Initial Validation

Data Docs displayed:

```text
fraud_schema

default
✓ Passed

drift_check
✗ Failed
```

---

## Failure Analysis

The failed expectation:

```text
ExpectColumnValuesToBeBetween

Column:
amount

Minimum Value:
0
```

Observed drift:

```text
Negative transaction amounts

Minimum Observed:
-347.22

Unexpected Values:
12
```

---

## Fix Applied

Updated the expectation:

```python
ExpectColumnValuesToBeBetween(
    column="amount",
    min_value=-400
)
```

The expectation was widened instead of being removed.

---

## Validation

Executed:

```bash
python3 /root/code/dataquality/fix_drift.py
```

Result:

```text
Checkpoint:
drift_check

Success:
True
```

---

## Verification

Confirmed:

```text
Expectation Suite:
Still Contains Four Expectations

Checkpoint:
Present

Validation JSON:
success = true

Data Docs:
Latest drift_check
Green
```

---

## Data Quality Workflow

```text
transactions_drifted.csv
        │
        ▼
Expectation Suite
        │
        ▼
Checkpoint
        │
        ▼
Data Docs
        │
Failure Investigation
        │
Expectation Widened
        │
        ▼
Checkpoint Passed
```

---

## Key Learning

Data Docs provides a clear debugging surface by identifying the exact expectation that failed, the observed values, and the deviation from expected bounds. Updating the expectation should be based on observed evidence rather than removing validation rules.

---

# Linux

# Forensic Collection of JavaScript Files

## Objective

Locate all JavaScript files under the eCommerce application directory and copy them to a secure investigation location while preserving the original directory structure.

---

## Source Directory

```text
/var/www/html/ecommerce
```

---

## Destination

```text
/ecommerce
```

---

## Commands Used

Created destination:

```bash
mkdir -p /ecommerce
```

Copied files while preserving parent directories:

```bash
cd /var/www/html

find ecommerce \
-type f \
-name "*.js" \
-exec cp --parents {} / \;
```

---

## Verification

Checked copied files:

```bash
find /ecommerce -type f -name "*.js"
```

Verified:

```text
Only .js files copied

Original directory structure preserved

Entire source directory not copied
```

---

## Architecture

```text
/var/www/html/ecommerce
          │
find *.js
          │
          ▼
cp --parents
          │
          ▼
/ecommerce
```

---

## Key Learning

The `cp --parents` option preserves the original directory hierarchy while copying only selected files, making it valuable for forensic evidence collection and selective backups.

---

# Kubernetes

# Creating a Kubernetes Job

## Objective

Create a Kubernetes Job template that executes a simple `sleep 5` command using the Ubuntu latest image.

---

## Job Configuration

```text
Job Name:
countdown-nautilus

Pod Template:
countdown-nautilus

Container Name:
container-countdown-nautilus

Image:
ubuntu:latest

Restart Policy:
Never
```

---

## Job Manifest

Created a Job with:

```text
apiVersion:
batch/v1

Kind:
Job
```

Configured container command:

```text
sleep 5
```

---

## Deployment

Applied:

```bash
kubectl apply -f countdown-nautilus.yaml
```

---

## Verification

Checked Job:

```bash
kubectl get jobs
```

Checked Pod:

```bash
kubectl get pods
```

Observed:

```text
Running

↓

Completed
```

after approximately five seconds.

---

## Job Execution Flow

```text
Job Created
      │
      ▼
Pod Scheduled
      │
      ▼
sleep 5
      │
      ▼
Pod Completed
      │
      ▼
Job Succeeded
```

---

## Key Learning

Kubernetes Jobs are intended for finite workloads. Unlike Deployments, they run to completion and terminate successfully once the configured task finishes.

---

# Quick Recall

```text
AWS
 ↓
SNS → Filter Policy → High & Low SQS Queues → Lambda Priority Processing

Azure
 ↓
Azure SQL Database → Export (BACPAC) → Blob Storage → Download

DevOps
 ↓
Docker → Python Image → Container → Port Mapping

MLOps
 ↓
Data Docs → Drift Detection → Widen Expectation → Successful Validation

Linux
 ↓
find → cp --parents → Preserve Directory Structure

Kubernetes
 ↓
Job → Ubuntu Container → sleep 5 → Completed
```

---

# Issues Encountered Today

### Great Expectations Drift Check

* The `drift_check` checkpoint failed because the `amount` column expectation required values to be greater than or equal to `0`, while the production dataset contained legitimate negative values.
* Resolved by widening the lower bound of the expectation to include the observed minimum while retaining the expectation.

### Docker Deployment

* Correct Docker layering was required to install dependencies before copying the application source.
* Verified successful deployment by exposing container port `5000` on host port `8097` and testing with `curl`.

### AWS Priority Queue

* Correct SNS subscription filter policies were essential to route messages into the appropriate SQS queues.
* Lambda logic ensured that high-priority messages were always processed before low-priority messages by polling the high-priority queue first.
