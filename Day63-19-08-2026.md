# Daily Learning Log — 19/08/2026 - Day63

# 1. DevOps (Kubernetes)

### Task

Deployed the Iron Gallery application and MariaDB database in a dedicated namespace, with the required ClusterIP and NodePort services.

### Namespace

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: iron-namespace-nautilus
```

### Gallery Deployment

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: iron-gallery-deployment-nautilus
  namespace: iron-namespace-nautilus
  labels:
    run: iron-gallery
spec:
  replicas: 1
  selector:
    matchLabels:
      run: iron-gallery
  template:
    metadata:
      labels:
        run: iron-gallery
    spec:
      containers:
        - name: iron-gallery-container-nautilus
          image: kodekloud/irongallery:2.0
          resources:
            limits:
              memory: 100Mi
              cpu: 50m
          volumeMounts:
            - name: config
              mountPath: /usr/share/nginx/html/data
            - name: images
              mountPath: /usr/share/nginx/html/uploads
      volumes:
        - name: config
          emptyDir: {}
        - name: images
          emptyDir: {}
```

### Database Deployment

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: iron-db-deployment-nautilus
  namespace: iron-namespace-nautilus
  labels:
    db: mariadb
spec:
  replicas: 1
  selector:
    matchLabels:
      db: mariadb
  template:
    metadata:
      labels:
        db: mariadb
    spec:
      containers:
        - name: iron-db-container-nautilus
          image: kodekloud/irondb:2.0
          env:
            - name: MYSQL_DATABASE
              value: database_host
            - name: MYSQL_ROOT_PASSWORD
              value: "NautilusRoot@2026#Db"
            - name: MYSQL_PASSWORD
              value: "NautilusUser@2026#Db"
            - name: MYSQL_USER
              value: ironuser
          volumeMounts:
            - name: db
              mountPath: /var/lib/mysql
      volumes:
        - name: db
          emptyDir: {}
```

### Database Service

```yaml
apiVersion: v1
kind: Service
metadata:
  name: iron-db-service-nautilus
  namespace: iron-namespace-nautilus
spec:
  type: ClusterIP
  selector:
    db: mariadb
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
```

### Gallery Service

```yaml
apiVersion: v1
kind: Service
metadata:
  name: iron-gallery-service-nautilus
  namespace: iron-namespace-nautilus
spec:
  type: NodePort
  selector:
    run: iron-gallery
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 32678
```

Apply all resources:

```bash
kubectl apply -f iron-gallery.yaml
```

Verify:

```bash
kubectl get namespace iron-namespace-nautilus
```

```bash
kubectl get deployments -n iron-namespace-nautilus
```

```bash
kubectl get pods -n iron-namespace-nautilus
```

```bash
kubectl get svc -n iron-namespace-nautilus
```

For troubleshooting:

```bash
kubectl get all -n iron-namespace-nautilus
```

```bash
kubectl describe pod -n iron-namespace-nautilus <pod-name>
```

```bash
kubectl logs -n iron-namespace-nautilus <pod-name>
```

### Key learning

Worked with Kubernetes namespaces, Deployments, resource limits, `emptyDir` volumes, environment variables, ClusterIP, and NodePort services.

---

# 2. MLOps — Asynchronous Fraud Prediction with Redis

### Task

Implemented Redis result persistence for an asynchronous Flask prediction service.

### Redis configuration

Existing configuration:

```python
REDIS = redis.Redis(host="localhost", port=6379, decode_responses=True)
```

### Worker result storage

Implemented:

```python
def _run_prediction(task_id: str, features) -> None:
    time.sleep(0.3)
    is_fraud = int(MODEL.predict(np.array([features]))[0])

    key = RESULT_KEY.format(task_id=task_id)
    REDIS.set(key, is_fraud, ex=RESULT_TTL_SECONDS)
```

This stores results as:

```text
result:<task_id>
```

with a:

```text
600 second TTL
```

### Result lookup

Implemented:

```python
@app.route("/result/<task_id>")
def result(task_id):
    key = RESULT_KEY.format(task_id=task_id)
    value = REDIS.get(key)

    if value is not None:
        return jsonify({
            "task_id": task_id,
            "is_fraud": int(value)
        }), 200

    return jsonify({
        "task_id": task_id,
        "status": "pending"
    }), 200
```

### Start application

```bash
cd /root/code/serving
python3 async_app.py
```

### Verify Redis

```bash
redis-cli ping
```

Expected:

```text
PONG
```

### Health check

```bash
curl http://localhost:8085/health
```

### Submit asynchronous prediction

```bash
curl -X POST http://localhost:8085/predict-async \
  -H "Content-Type: application/json" \
  -d '{"amount":3200,"hour":23,"num_tx_past_day":5}'
```

Returned:

```json
{"task_id":"<task_id>"}
```

### Poll result

```bash
curl http://localhost:8085/result/<task_id>
```

If processing isn't finished:

```json
{
  "status": "pending",
  "task_id": "<task_id>"
}
```

After processing:

```json
{
  "is_fraud": 1,
  "task_id": "<task_id>"
}
```

or:

```json
{
  "is_fraud": 0,
  "task_id": "<task_id>"
}
```

### Redis verification

```bash
redis-cli keys 'result:*'
```

```bash
redis-cli get 'result:<task_id>'
```

```bash
redis-cli ttl 'result:<task_id>'
```

### Key learning

Implemented an asynchronous ML inference workflow where the HTTP request immediately returns a task ID, while the background worker performs inference and persists the result in Redis for later polling.

---

# 3. Terraform — EC2 + S3 + IAM Role

### Task

Created an EC2 instance with an IAM role allowing secure S3 object uploads.

### Resources

```text
EC2:             devops-ec2
S3:              devops-logs-18466
IAM Role:        devops-role
IAM Policy:      devops-access-policy
```

### `variables.tf`

```hcl
variable "KKE_BUCKET_NAME" {
  description = "Name of the S3 bucket"
  type        = string
}

variable "KKE_POLICY_NAME" {
  description = "Name of the IAM policy"
  type        = string
}

variable "KKE_ROLE_NAME" {
  description = "Name of the IAM role"
  type        = string
}
```

### `terraform.tfvars`

```hcl
KKE_BUCKET_NAME = "devops-logs-18466"
KKE_POLICY_NAME = "devops-access-policy"
KKE_ROLE_NAME   = "devops-role"
```

### `data.tf`

```hcl
data "aws_ami" "amazon_linux_2" {
  most_recent = true

  owners = ["amazon"]

  filter {
    name   = "name"
    values = ["amzn2-ami-hvm-*-x86_64-gp2"]
  }

  filter {
    name   = "state"
    values = ["available"]
  }
}
```

### `main.tf`

```hcl
resource "aws_s3_bucket" "devops_logs" {
  bucket = var.KKE_BUCKET_NAME
}

resource "aws_iam_role" "devops_role" {
  name = var.KKE_ROLE_NAME

  assume_role_policy = jsonencode({
    Version = "2012-10-17"

    Statement = [
      {
        Effect = "Allow"

        Principal = {
          Service = "ec2.amazonaws.com"
        }

        Action = "sts:AssumeRole"
      }
    ]
  })
}

resource "aws_iam_policy" "devops_access_policy" {
  name = var.KKE_POLICY_NAME

  policy = jsonencode({
    Version = "2012-10-17"

    Statement = [
      {
        Effect = "Allow"

        Action = [
          "s3:PutObject"
        ]

        Resource = "${aws_s3_bucket.devops_logs.arn}/*"
      }
    ]
  })
}

resource "aws_iam_role_policy_attachment" "devops_policy_attachment" {
  role       = aws_iam_role.devops_role.name
  policy_arn = aws_iam_policy.devops_access_policy.arn
}

resource "aws_iam_instance_profile" "devops_profile" {
  name = "${var.KKE_ROLE_NAME}-profile"
  role = aws_iam_role.devops_role.name
}

resource "aws_instance" "devops_ec2" {
  ami           = data.aws_ami.amazon_linux_2.id
  instance_type = "t2.micro"

  iam_instance_profile = aws_iam_instance_profile.devops_profile.name

  tags = {
    Name = "devops-ec2"
  }
}
```

### Terraform commands

```bash
terraform init
```

```bash
terraform fmt
```

```bash
terraform validate
```

```bash
terraform plan
```

```bash
terraform apply
```

### Verification

```bash
terraform state list
```

```bash
aws s3api head-bucket --bucket devops-logs-18466
```

```bash
aws iam get-role --role-name devops-role
```

```bash
aws iam list-policies \
  --query "Policies[?PolicyName=='devops-access-policy'].{Name:PolicyName,Arn:Arn}"
```

### Key learning

Used an IAM role and instance profile instead of storing AWS access keys on the EC2 instance. The role grants only `s3:PutObject` against objects in the required S3 bucket.

---

# 4. Linux — Apache Security Headers

### Task

Hardened Apache on **App Server 3** by changing its listening port and adding security response headers.

### Step 1 — Install Apache

```bash
sudo yum install -y httpd
```

Verify:

```bash
httpd -v
```

### Step 2 — Configure port

Edited:

```bash
sudo vi /etc/httpd/conf/httpd.conf
```

Changed:

```apache
Listen 80
```

to:

```apache
Listen 8082
```

### Step 3 — Create website content

```bash
sudo mkdir -p /var/www/html
```

Created:

```bash
sudo vi /var/www/html/index.html
```

Content:

```text
Welcome to the xFusionCorp Industries!
```

### Step 4 — Configure security headers

Added to `/etc/httpd/conf/httpd.conf`:

```apache
Header always set X-XSS-Protection "1; mode=block"
Header always set X-Frame-Options "SAMEORIGIN"
Header always set X-Content-Type-Options "nosniff"
```

Checked the headers module:

```bash
httpd -M | grep headers
```

Expected:

```text
headers_module (shared)
```

### Step 5 — Validate configuration

```bash
sudo apachectl configtest
```

Expected:

```text
Syntax OK
```

### Step 6 — Start Apache

```bash
sudo systemctl enable --now httpd
```

Check:

```bash
sudo systemctl status httpd
```

### Step 7 — Verify port

```bash
sudo ss -tlnp | grep 8082
```

### Step 8 — Test response headers

```bash
curl -I http://localhost:8082/
```

Expected headers:

```text
X-XSS-Protection: 1; mode=block
X-Frame-Options: SAMEORIGIN
X-Content-Type-Options: nosniff
```

Verify page content:

```bash
curl http://localhost:8082/
```

Expected:

```text
Welcome to the xFusionCorp Industries!
```